import React from "react";

const Title = ({ title }) => {
  return (
    <>
      <h2 className="font-primaryFont font-bold text-[40px] text-black">
        {title}
      </h2>
    </>
  );
};

export default Title;
